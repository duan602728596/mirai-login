{
  "name": "@mirai-login/main",
  "version": "0.0.1",
  "scripts": {
    "start": "npm run clean && babel src --out-dir lib --watch",
    "dev": "npm run clean && babel src --out-dir lib",
    "build": "npm run clean && cross-env NODE_ENV=production gulp",
    "clean": "rimraf ./lib"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.0",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-terser": "^1.4.0"
  }
}
